version: "3.8"

services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "${APP_PORT}:8000"
    env_file:
      - ../.env
    volumes:
      - ../app:/app/app # Optional: hot-reloading during dev
      - /mnt/data/binarysuccess/orchestration/files/wallet:/app/wallet
